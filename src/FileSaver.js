import { $ } from "./utils.js";

class FileSaver {
    static #downloader = $("#_downloader");
    /** @param {Blob} blob @param {string} fileName */
    static async save(blob, fileName) {
    }
    /** @returns {Blob} */
    static load() {
        
    }
}

export default FileSaver;